{
  "name": "@airgram/core",
  "version": "5.1.2",
  "private": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "keywords": [
    "telegram",
    "tdlib",
    "telegram bot",
    "telegram client",
    "telegram javascript",
    "telegram typescript"
  ],
  "scripts": {
    "build": "gulp build",
    "check": "tsc --noEmit",
    "lint": "echo \"ignore eslint\"",
    "prepublishOnly": "yarn run build",
    "test": "echo \"Error: no test specified\""
  },
  "pre-commit": "yarn run lint",
  "publishConfig": {
    "access": "public"
  }
}
