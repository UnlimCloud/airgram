{
  "name": "airgram",
  "version": "5.1.2",
  "private": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "keywords": [
    "telegram",
    "tdlib",
    "telegram bot",
    "telegram client",
    "telegram javascript",
    "telegram typescript"
  ],
  "scripts": {
    "build": "gulp build ",
    "dev": "nodemon --config nodemon.json",
    "check": "tsc --noEmit",
    "lint": "echo \"ignore lint\"",
    "prebuild": "npm run test",
    "prepublishOnly": "gulp build",
    "test": "echo \"Error: no test specified\""
  },
  "pre-commit": "yarn run lint",
  "dependencies": {
    "@airgram/constants": "^4.1.2",
    "@airgram/core": "^5.1.2",
    "ffi-napi": "^4.0.3"
  },
  "devDependencies": {
    "@types/ffi-napi": "^4.0.10",
    "fast-glob": "^3.0.4",
    "nodemon": "^1.19.1"
  },
  "engines": {
    "node": ">=12"
  }
}
